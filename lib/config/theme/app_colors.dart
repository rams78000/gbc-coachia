import 'package:flutter/material.dart';

/// Couleurs de l'application
class AppColors {
  /// Couleur primaire
  static const Color primary = Color(0xFF4A6FFF);
  
  /// Couleur primaire en dark mode
  static const Color primaryDark = Color(0xFF6989FF);
  
  /// Couleur secondaire
  static const Color secondary = Color(0xFF12B76A);
  
  /// Couleur d'accentuation
  static const Color accent = Color(0xFF7F56D9);
  
  /// Couleur d'arrière-plan
  static const Color background = Color(0xFFFAFAFA);
  
  /// Couleur d'arrière-plan en dark mode
  static const Color backgroundDark = Color(0xFF121212);
  
  /// Couleur de surface
  static const Color surface = Colors.white;
  
  /// Couleur de surface en dark mode
  static const Color surfaceDark = Color(0xFF1E1E1E);
  
  /// Couleur d'erreur
  static const Color error = Color(0xFFEF4444);
  
  /// Couleur de texte
  static const Color text = Color(0xFF1A1A1A);
  
  /// Couleur de texte en dark mode
  static const Color textDark = Color(0xFFF5F5F5);
  
  /// Couleur de texte secondaire
  static const Color textSecondary = Color(0xFF6B7280);
  
  /// Couleur de texte secondaire en dark mode
  static const Color textSecondaryDark = Color(0xFFABABAB);
  
  /// Couleur de bordure
  static const Color border = Color(0xFFE5E7EB);
  
  /// Couleur de bordure en dark mode
  static const Color borderDark = Color(0xFF2C2C2C);
}
